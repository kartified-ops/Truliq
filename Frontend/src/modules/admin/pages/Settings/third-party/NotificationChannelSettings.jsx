import React from 'react';
import SimpleProviderCards from './SimpleProviderCards';

const NotificationChannelSettings = () => (
  <SimpleProviderCards
    serviceName="notification_channel"
    title="Notification Channel Settings"
    description="Configure notification channel providers."
  />
);

export default NotificationChannelSettings;
