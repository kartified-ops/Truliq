import React from 'react';
import SimpleProviderCards from './SimpleProviderCards';

const MapsSettings = () => (
  <SimpleProviderCards
    serviceName="maps"
    title="Map & Map APIs Settings"
    description="Configure map provider API keys for location and distance services."
  />
);

export default MapsSettings;
